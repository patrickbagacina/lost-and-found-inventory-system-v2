class CreateItemCmd {
  final String category;
  final String itemType;
  final String brand;
  final String description;

  CreateItemCmd(this.category, this.itemType, this.brand, this.description);
}
